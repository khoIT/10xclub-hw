# given an array like [3, -2, 7, 9, 8, 1, 2, 0, -1, 5, 8], return its largest subset such that
# if two integers are in the subset then so are all integers between them. doesn't have to be consecutive

def longest_subset(self):
